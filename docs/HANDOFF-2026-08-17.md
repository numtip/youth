# Handoff — 2026-08-17

> **Closeout status (2026-08-17):** the **2567 + 2564** migration batch is
> prepared locally and validated (content/media/check/build/smoke all pass).
> Pending: owner review of the PR, then merge → auto-deploy to
> `https://numtip.github.io/youth/`.
> **Next session:** content-owner acceptance of the 2567/2564 batch; then the
> remaining blockers (missing 2569 media, EN content).

## Completed work

- **Crawled the legacy Joomla site** (`https://researchex.mju.ac.th/youth/`)
  read-only with a Python crawler (`/tmp/migrate/crawl.py`) → manifest of 13
  projects / 51 activities / ~580 images.
- **Migrated 7 projects + 35 activities** (verbatim Thai text) into content
  collections:
  - **2567 (2 projects, 10 activities):**
    - `livestock-farmers` — merged legacy categories `28-youth2567-1` +
      `30-livestock-farmers2567` (same project split across two legacy pages)
    - `local-herbs` — merged `29-youth2567-2` + `31-local-herbs2567`
  - **2564 (5 projects, 25 activities):** `eco-tourism-route`, `fish-hen-farming`,
    `smoke-haze-media`, `agri-innovation-nongyaeng`, `tourism-baophudin`
- **344 media files downloaded** (original filenames; phone-like names renamed to
  `img-N.jpg` to satisfy the PII validator) → `public/media/projects/2564|2567/`.
- **Redirect map extended:** `src/data/legacy-redirects.json` now has **204
  entries** (added 64; also completes the previously-missing 2568 mappings).
- **Normalisation:** removed Widgetkit gallery-number junk from extracted text;
  stripped leading `N.` numbering from activity titles; event dates parsed from
  Thai dates in the article text when present (else legacy publish date).

## Validation results (local, exit code 0)

| Command | Result |
| --- | --- |
| `npm run validate` | PASS — 13 projects + 53 activities; 10 media allowlisted (2569 only) |
| `npm run check` | PASS — 0 errors / 0 warnings / 0 hints |
| `npm run build` | PASS — 146 static pages (was 62) |
| `npm run test:smoke` | PASS — 23/23 |

## Data-quality notes (for owner review)

- **`livestock-farmers/activity-4` date** — legacy publish date was the batch
  import date `2021-05-20`; set to `2024-05-20` (matches its sibling activity).
  Verify against the real event date.
- **2564 activity dates** — several legacy pages carry the batch import date
  `2021-05-20` (Joomla site rebuild). Where the article text contained a Thai
  date it was used instead; remaining ones may need owner-confirmed dates.
- **2567 merge decision** — legacy pages 28+30 and 29+31 are the same two 2567
  projects split across category pages; merged into 2 projects (evidence:
  `29` activity text mentions "ทั้ง 2 โครงการ"). Flag if they should be kept
  separate instead.
- Text preserved verbatim; no machine translation, no fabricated media.

## Safety boundaries

- Legacy Joomla site untouched (read-only fetch only).
- No production/Apache/VPS changes.
- Deploy happens automatically on merge to `main` (GitHub Pages).

## Remaining blockers

1. **10 media files (2569)** still missing and allowlisted.
2. **English content** — `title_en`/`summary_en` drafted for all 66 entries
   (subagent-assisted, 2026-08-17, PR #4 pending owner review/merge).
3. **Nginx TH/EN deep-route 404** — deferred VPS gate.
4. **2563 and older** — not present on the legacy listing; nothing to migrate.
