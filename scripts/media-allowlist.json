{
  "_note": "TEMPORARY allowlist for referenced media that has not been migrated yet. These paths are expected to exist under public/ once Phase 2 media migration is complete. Remove each entry (or the whole file) as the real files are added; missing media that is not allowlisted will fail `npm run validate:media`.",
  "paths": []
}