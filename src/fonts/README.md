# Sarabun (self-hosted)

Sarabun by Cadson Demak. SIL Open Font License 1.1.

Source: Google Fonts `Sarabun` v17, weights **400 / 600 / 700**, subsets **Thai + Latin** (the languages this site publishes).

Files are referenced from `src/styles/global.css` so Vite emits them with the configured `base` (local `/` or GitHub Pages `/youth/`).
